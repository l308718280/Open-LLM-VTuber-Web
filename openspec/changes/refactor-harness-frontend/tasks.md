## 1. 契约与发布前置确认

- [ ] 1.1 与 Harness 负责人确认实际生成地址、协议 version=1、请求字段及事件字段类型；形成可执行契约样例，明确只提交本次图文，不上传历史、记忆、提示词或工具定义。
- [ ] 1.2 确认鉴权用户归属、短期身份策略、CORS/CSP 和代理来源配置；会话 ID 不作为凭证，远端使用 HTTPS，不以关闭 TLS 或浏览器安全校验解决接入。
- [ ] 1.3 确认后端请求去重期限、同会话串行、busy 行为及代理断开向模型/可取消工具传播能力；不满足时阻塞发布并重新评估单入口契约。
- [ ] 1.4 确认服务端图片留存、长期记忆写入及工具副作用告知内容；前端不得宣称停止等待等于回滚或图片不会被后端保存。
- [ ] 1.5 阅读候选模型 mao_pro、shizuku 的 ReadMe 与再分发条款，核对背景/头像许可及必要运行文件；确定获准发布的默认资源，许可不明时不得复制发布。
- [ ] 1.6 核查现有协议调用、Provider、存储键、音频依赖、构建复制规则及 Electron IPC 引用，记录待删与保留边界；仅做基线检查，不将旧缺陷算作本次通过项。

## 2. 最小验证入口与可控模拟流

- [ ] 2.1 核查现有工程脚本并选择适配 TypeScript/React 的最小自动测试入口，配置运行命令；只增加本变更必需依赖，不假定已有根项目测试脚本。
- [ ] 2.2 建立可控 fetch/SSE 测试替身，支持字节分块、延迟、心跳、终态、断流和中止；用一个正常样例验证测试入口可执行。
- [ ] 2.3 建立可控时钟与延迟截图替身，能检测在途轮次、草稿变更、轨道停止和位图释放；验证替身不会触发真实权限或外部服务。
- [ ] 2.4 添加最小合法/非法协议样例，覆盖全部事件类型、静默、标识错配和大小边界；样例不得含真实聊天、图片或凭证。
- [ ] 2.5 每次仅生成并验证一个必要测试文件，先修复该文件类型及执行问题再继续；将关键规约场景对应到自动检查或明确手工步骤。
- [ ] 2.6 记录独立的类型检查、无修复 ESLint、Web 与 Electron 构建命令及基线结果；现有 lint 脚本含修复选项，不用于只读验收。

## 3. Harness 协议客户端

- [ ] 3.1 定义请求、事件与安全错误类型及运行时校验；校验触发类型、图文有效性和标识，限制观察为一张屏幕图，不接入旧 WebSocket 双栈。
- [x] 3.2 实现增量 UTF-8 解码与 SSE 组帧，支持 CRLF、空行、多行 data、注释和连续事件；验证中文任意字节分块、帧边界及终止残片不丢字或重复。
- [ ] 3.3 实现 accepted 首事件、从1连续 seq、重复忽略、跳号失败、会话错配失败及其他请求隔离；未知可选事件消费序号，终态后禁止更新。
- [ ] 3.4 实现 fetch POST 与响应状态/媒体类型验证，单事件及未完成帧上限1 MiB、累计回复上限1 MiB；覆盖非法 JSON、非2xx、非流响应和超限安全报错。
- [ ] 3.5 实现首事件30秒、空闲90秒、总时长10分钟计时及终态幂等清理；验证心跳仅重置空闲、无终态断流失败、busy 和网络错误不自动重放 POST。
- [ ] 3.6 实现 done/error 与 outcome 校验、公开状态白名单和安全错误映射；验证 silent 仅允许未输出文字的自动观察，不暴露内部推理、原始响应或工具敏感字段。

## 4. 请求协调器、临时对话与字幕

- [ ] 4.1 建立页面内存会话与临时对话状态，安全随机生成 session_id/request_id；首次确认前取消仍复用会话，刷新及保存不同服务地址创建新会话且不恢复正文。
- [ ] 4.2 实现 idle/capturing/requesting/streaming 阶段与 completed/cancelled/failed 结算；由单一协调器持有准备锁、活动请求和 generation，模型状态独立。
- [ ] 4.3 实现所有入口统一取消：先失效令牌，再中止网络/读流并清理计时器与缓冲；覆盖停止、卸载、地址切换及旧清理迟到，不把新轮置为空闲。
- [ ] 4.4 实现输入快照、同步准备锁和来源快照；验证组合输入、Shift+Enter、空白/纯图片、慢截图重复提交和新草稿保护，准备失败不清草稿、不降级纯文字。
- [ ] 4.5 实现用户提交抢占活动网络轮、失败保留部分回复和显式新 request_id 重试；编辑本身不取消回复，组合输入及编辑后2秒仅抑制自动观察。
- [ ] 4.6 将文字增量直接接入本轮唯一回复与字幕，初始字幕为空、首次非空增量替换旧字幕；覆盖静默、空增量、取消及失败保留正文，状态与正文分离并安全渲染。

## 5. 本地资源与无音频 Live2D

- [ ] 5.1 仅复制已获许可的完整模型运行资源及必要声明，建立稳定 ID、头像、相对入口、布局和有效动作/表情映射清单；检查引用完整且无开发机绝对路径。
- [ ] 5.2 接入可发布本地背景/纯色与模型选择，从清单初始化并只持久化允许偏好；验证未知 ID 回退、尺度幂等及 Web/Electron 应用基准地址解析。
- [ ] 5.3 修复重载延时清理、异步身份隔离及模型释放；验证快速切换、卸载、旧加载迟到，以及模型不可用时文字与停止仍可操作。
- [ ] 5.4 修复零动作、全部可选动作失败和最后动作失败的完成检查；必要模型/纹理失败显示可重试错误，验证不会永久卡在加载阶段。
- [ ] 5.5 建立流展示独立适配器，校验本地映射及当前模型/轮次；保留动作优先级规则，补载失败仅释放本次预留，表情采用有效默认/首个有效/无操作回退。
- [ ] 5.6 移除应用音频、PCM/波形口型和音频调试入口，保留 Cubism Core、通用 SDK 及非音频调试并清理卸载入口；回归既有点击、拖动、滚轮与布局行为。

## 6. 媒体授权、截图与图文接入

- [ ] 6.1 整理主/背景摄像头独立生命周期，默认关闭、仅请求视频、启动互斥；覆盖无 API/无设备/拒绝授权/迟到启动，停止或卸载释放轨道且不影响另一来源。
- [ ] 6.2 整理浏览器屏幕共享和 Electron 源选择链，关闭音频并显示来源及停止状态；覆盖取消选择、外部 ended、迟到流释放和启动锁恢复。
- [ ] 6.3 将捕获器构造、配置读取、抓帧及编码纳入异常边界，补齐 ImageCapture 不可用时就绪视频加 Canvas 回退；验证位图在成功、失败和过期时均释放。
- [ ] 6.4 实现 JPEG 默认质量0.8、范围0.1至1，最长边默认1280、范围320至2048且不放大；迁移旧0值并校验非法参数，验证图片等比缩放。
- [ ] 6.5 实现单图原始编码字节2 MiB、总图片4 MiB及序列化 JSON 正文6 MiB限制；覆盖边界和超限拒绝，删除图片正文日志且不静默截断。
- [ ] 6.6 接入普通提交明确选择的主摄像头后屏幕图片，每源最多一张，背景摄像头永不上传；验证所选来源失败保留草稿、授权撤销及迟到截图不发送或写入 UI。

## 7. 手动与自动屏幕观察

- [ ] 7.1 新增手动观察及默认关闭、不持久化的自动观察控制，展示发送目标、持续采样告知、来源、结果和停止入口；无有效授权时只提示，不隐式申请权限。
- [ ] 7.2 经统一协调器提交 manual-observation/auto-observation，仅附一张屏幕图；手动观察不受自动变化阈值限制，自动工作不得抢占用户请求。
- [ ] 7.3 实现默认30秒、范围10至300秒的检查间隔及至少30秒启动冷却；忙、组合输入、编辑抑制或隐藏时跳过，不排队，可见后重新计时。
- [ ] 7.4 实现64乘36灰度平均绝对差除以255，默认阈值0.03、范围0.01至1；验证无基线首帧可发送、低差异跳过、源或原图尺寸变化清基线。
- [ ] 7.5 仅以成功终结的自动请求帧更新基线，含 silent；验证失败、取消、手动观察不更新，旧源迟到结果不覆盖新基线，比较帧只保存在内存。
- [ ] 7.6 实现停止自动仅取消自动工作、停止共享使观察失效并释放流；连续3次自动失败关闭，成功或重新开启重置计数，跳过/抢占/主动取消不计失败并覆盖竞态。

## 8. 设置事务化与轻量壳层

- [ ] 8.1 将保留偏好统一为已提交状态和每次打开的新草稿；保存全部校验成功再提交，取消/叉号/遮罩/Escape 一律丢弃，覆盖连续打开和旧闭包问题。
- [ ] 8.2 实现本地模型/背景显式预览及取消回退，设备启停保持独立明确操作；验证回退或刷新不申请权限，不把历史设备开关覆盖到实际流状态。
- [ ] 8.3 实现服务地址校验及保存时切换，统一取消旧工作、清临时对话并换会话；编辑地址不立即生效，不向新服务发送旧内容。
- [ ] 8.4 统一存储读取、解析、校验和写入异常处理及最新状态函数式更新；只迁移/删除已识别旧键，保留无关数据，存储失败回退内存并安全提示。
- [ ] 8.5 重组 Provider 与组件依赖为本地偏好、媒体、模型、Harness、观察及字幕，删除旧空壳状态；保留主题、语言、模式、窗口/桌宠与媒体面板。
- [ ] 8.6 以请求状态替换连接状态并补齐保留界面文案；验证离线初始化、模型失败、生成服务不可达时仍可编辑及控制窗口，不持久化草稿/聊天/图片/自动开启状态/长效密钥。

## 9. 旧功能、协议与构建资源裁剪

- [ ] 9.1 删除群组及历史管理 UI、Provider、Hooks、协议路由与专属类型，保留新的内存对话展示；检索确认不再创建、切换、删除或拉取后端历史。
- [ ] 9.2 删除远端角色/背景初始化、旧 WebSocket 服务及旧工具浏览器自动打开链；核查所有调用方，确认无旧协议重连或后台初始化请求。
- [ ] 9.3 删除麦克风/VAD/ASR、音频上传/播放/回执、音频队列及旧主动说话链；通用队列仅在无其他用途时删除，检索确认无旧4096样本及中断消息路径。
- [ ] 9.4 核对 Electron 主进程和 preload 的工具浏览器 IPC 与类型引用，仅删除无剩余用途部分；保留桌面源选择、窗口/桌宠和既有安全边界。
- [ ] 9.5 核对并移除仅用于语音的依赖、VAD/ONNX/worklet/PCM 资源及两套构建复制规则，同步锁文件；检查间接引用并确保 Cubism Core 和必要模型资源仍打包。
- [ ] 9.6 清理失效设置、翻译、类型和 import；对运行入口及制品做残留检查，确认无旧 WebSocket、麦克风授权、远端工具页面自动打开或敏感正文日志。

## 10. 回归、真实联调与发布门槛

- [ ] 10.1 运行已建立的必要自动测试，覆盖协议分块/顺序/超限/超时、取消迟到、慢截图草稿、观察基线及设置迁移；区分模拟通过与真实设备/服务待验。
- [ ] 10.2 顺序执行 npm run typecheck、直接不带修复选项的 ESLint、npm run build:web、npm run build、npm run build:unpack；记录实际结果并修复本次引入问题。
- [ ] 10.3 在浏览器与 Electron 窗口/桌宠验收本地资源、获准模型切换、坏动作/纹理、离线展示和输入字幕；检查解包资源路径及点击/拖动/滚轮回归。
- [ ] 10.4 在真实设备验证摄像头独立性、屏幕源选择、拒绝及撤销授权、隐藏恢复、停止抓帧竞态和背景摄像头不上传；通过网络/日志/存储检查图片限制及隐私边界。
- [ ] 10.5 经实际 Harness 与部署代理验证首事件、持续流、心跳、断流、取消传播、请求去重、同会话串行、busy、身份及 CORS/CSP；未达标项阻塞发布，不以本地 Abort 或模拟通过替代。
- [ ] 10.6 核对许可、资源完整性、图片政策和全部发布门槛后按协议版本协同发布；验证旧前端制品配套旧后端的回退，不保留双栈；实施验收后再同步/归档规约，保留历史说明但不作为新实现证据。